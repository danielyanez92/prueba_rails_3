<%= form_with(model: [@company], html: { class: 'form-horizontal' }) do |f| %>
  <div class="form-group">
    <div class="col-md-12">
      <%= f.text_field :name, class: 'form-control' %>
    </div>
  </div>
  <%= f.submit 'Confirmar', class: 'btn btn-primary' %>
<% end %>
